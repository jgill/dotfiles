name: Nukem
root: projects/personal/nukem

pre: gitx; ctags --exclude=vendor -R .

windows:
  - editor: vim .
  - database: mysql -u root nukem_development
  - console: bundle exec rails c
  - server: bundle exec rails s -p 3000
  - utility: clear
  - logs: tail -f log/development.log
