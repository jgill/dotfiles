name: bingo
root: /Users/johngill/projects/research/elm/pragstudio-elm-code/starter-project/bingo

pre: gitx; ctags --exclude=elm-stuff -R .

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
# pre_window: rbenv shell 2.0.0-p247

windows:
  - editor: vim .
  - repl: elm repl
  - reactor: elm-live Bingo.elm --open --output=bingo.js
  - utility: clear

